# MaxGamer 环境变量配置文件
# 复制此文件为 .env 并填入实际值

# ========== 基础配置 ==========
FLASK_ENV=production
SECRET_KEY=your-secret-key-change-this-to-random-string
JWT_SECRET=your-jwt-secret-change-this-to-random-string

# ========== 服务端口配置 ==========
BACKEND_PORT=3000   # 后端 API 端口
FRONTEND_PORT=8080  # 前端静态文件端口

# ========== 数据库配置（PostgreSQL 隔离） ==========
DB_NAME=maxgamer
DB_USER=maxgamer_user
DB_PASSWORD=change_this_password_to_secure_one
# SQLite 模式（简单部署）
# DATABASE_URL=sqlite:///data/frameworker.db
# PostgreSQL 模式（生产环境推荐）
DATABASE_URL=postgresql://maxgamer_user:change_this_password_to_secure_one@maxgamer-db:5432/maxgamer

# ========== Redis 缓存配置（隔离） ==========
REDIS_PASSWORD=redis_password_change_this
REDIS_HOST=maxgamer-redis
REDIS_PORT=6379

# ========== Twitch OAuth 配置 ==========
TWITCH_CLIENT_ID=3tfkf4ohu90fmn5v6r339cgim5task
TWITCH_CLIENT_SECRET=your_twitch_client_secret_here
TWITCH_REDIRECT_URI=https://your-domain.com/api/auth/platform-callback/twitch

# ========== 邮件配置（如使用邮箱验证）==========
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# ========== 短信配置（如使用短信验证）==========
# SMS_API_KEY=your-sms-api-key
# SMS_API_SECRET=your-sms-api-secret

# ========== 游戏隔离配置 ==========
GAME_LIBRARY_PATH=/app/GameLibrary
GAME_DATA_PATH=/app/data/games
GAME_SANDBOX_MODE=true  # 启用游戏沙箱隔离

# ========== 其他配置 ==========
BCRYPT_LOG_ROUNDS=12
