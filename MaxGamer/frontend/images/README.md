# 图片资源目录

## Logo图片

请将您的Logo图片命名为 `logo.png` 并放置在此目录中。

### 建议规格
- 格式：PNG（支持透明背景）
- 尺寸：建议宽度200px，高度60px以内
- 文件大小：建议小于100KB

### 使用位置
- 登录页面（`/login.html`）

### 备用方案
如果Logo图片不存在或加载失败，页面会自动显示默认的文字Logo（🎬 MaxGamer）。